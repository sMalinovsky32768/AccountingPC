<Window x:Class="AccountingPC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AccountingPC"
        mc:Ignorable="d"
        Title="Авторизация" Height="200" Width="450" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Логин" Style="{Binding Mode=OneWay, Source={StaticResource TextBlockStyle}}"/>
        <TextBox Grid.Column="1" x:Name="loginTextBox" Style="{Binding Mode=OneWay, Source={StaticResource TextBoxStyle}}"/>
        <TextBlock Grid.Row="1" Text="Пароль" Style="{Binding Mode=OneWay, Source={StaticResource TextBlockStyle}}"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="passwordTextBox" Style="{Binding Mode=OneWay, Source={StaticResource PasswordBoxStyle}}"/>
        <Button Grid.Row="2" Grid.Column="1" Content="Войти" Click="Login_Click" IsDefault="True"/>
    </Grid>
</Window>
